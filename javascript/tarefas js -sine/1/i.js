//1 – Calcule a média de diversas notas digitadas pelo usuário.
function calcularMedias(notas_string){
    var notas = notas_string.split(",");
}

console.log("QUERO SEXO");

function calcularMedias(notas){
    let soma = 0;
    for (let i = 0; i< notas.length; i++) {
        soma = soma + Number(notas[i]);
        
    }
}

var notas = prompt("Digite notas separadas por vírgula ", 0);
console.log(calcularMedias(notas))